// Real contact channels confirmed from the client's own event flyers
// (extras/clients/mlt/assets/). No email address or physical location has been provided by
// the client yet — deliberately not shown anywhere rather than fabricated; add once supplied.
export const WHATSAPP_NUMBER = "+254720450565";
export const INSTAGRAM_HANDLE = "menletstalk254";

export const WHATSAPP_URL = `https://wa.me/${WHATSAPP_NUMBER.replace("+", "")}`;
export const INSTAGRAM_URL = `https://instagram.com/${INSTAGRAM_HANDLE}`;
